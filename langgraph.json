{
  "$comment": "Store config for LangGraph Cloud deployments. For local dev, pass embeddings directly to Store.",
  "store": {
    "index": {
      "embed": "openai:text-embedding-3-small",
      "dims": 1536,
      "fields": ["content", "url"]
    }
  }
}
